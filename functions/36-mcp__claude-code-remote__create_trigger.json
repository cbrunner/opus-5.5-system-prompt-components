{
  "description": "Create a scheduled task. Each firing starts a fresh session. Use send_later for a reminder in THIS conversation.",
  "name": "mcp__claude-code-remote__create_trigger",
  "parameters": {
    "properties": {
      "cron_expression": {"description": "5-field cron in UTC. Mutually exclusive with run_once_at.", "type": "string"},
      "environment_id": {"type": "string"},
      "initiation": {"enum": ["human_request", "human_schedule", "own_followup", "own_initiative"], "type": "string"},
      "name": {"type": "string"},
      "prompt": {"description": "Message each firing sends.", "type": "string"},
      "run_once_at": {"description": "RFC3339 one-shot fire time.", "type": "string"}
    },
    "required": ["name", "prompt"],
    "type": "object"
  }
}
