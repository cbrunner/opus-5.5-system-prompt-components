{
  "description": "Show your single best product pick as one rich card with a name, optional price, and a blurb.",
  "name": "mcp__widgets__featured_card_display_v0",
  "parameters": {
    "properties": {
      "products": {
        "items": {
          "properties": {
            "blurb": {"type": "string"},
            "name": {"type": "string"},
            "price": {"type": "string"},
            "url": {"type": "string"}
          },
          "required": ["name"],
          "type": "object"
        },
        "maxItems": 1,
        "minItems": 1,
        "type": "array"
      },
      "summary": {"type": "string"}
    },
    "required": ["products", "summary"],
    "type": "object"
  }
}
