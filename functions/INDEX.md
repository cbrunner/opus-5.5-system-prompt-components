# Functions in the Opus 5.5 system prefix

- `Agent` — Launch a new agent to handle complex, multi-step tasks. Each agent type has specific capabilities and tools available to it. (`01-Agent.json`)
- `Artifact` — The Artifact tool renders an HTML file as an Artifact: a web page hosted on claude.ai that is private by default. Claude uses it when a page would be clearer than text in the conve (`02-Artifact.json`)
- `AskUserQuestion` — Use this tool only when you are blocked on a decision that is genuinely the user's to make: one you cannot resolve from the request, the code, or sensible defaults. (`03-AskUserQuestion.json`)
- `Bash` — Executes a bash command and returns its output. (`04-Bash.json`)
- `Edit` — Performs exact string replacement in a file. (`05-Edit.json`)
- `Glob` — Fast file pattern matching. Supports glob patterns like "**/*.js" or "src/**/*.ts". Returns matching file paths sorted by modification time. (`06-Glob.json`)
- `Grep` — Content search built on ripgrep. Prefer this over `grep`/`rg` via Bash — results integrate with the permission UI and file links. (`07-Grep.json`)
- `ListAgents` — Lists agents you can SendMessage to — in-process subagents you spawned, the teammates on your team, other local Claude sessions on this machine, your Claude sessions running in the (`08-ListAgents.json`)
- `propose_skills` — Surface recurring multi-step procedures from this session as skill proposals. Render-only — calling this shows a review card in the conversation; it does not write any files or cre (`09-propose_skills.json`)
- `Read` — Reads a file from the local filesystem. (`10-Read.json`)
- `ReadNotifications` — Read the notifications queued for this session — GitHub activity on subscribed PRs, scheduled triggers (including check-ins you scheduled yourself), and messages from other Claude  (`11-ReadNotifications.json`)
- `RefreshMcpTools` — Re-query the tool lists of connected MCP servers and update the available tools. (`12-RefreshMcpTools.json`)
- `ReportFindings` — Report code-review findings as a typed list so the host UI can render them. Use this only when the active code-review instructions tell you to report findings with this tool; other (`13-ReportFindings.json`)
- `ScheduleWakeup` — Schedule when to resume work in /loop dynamic mode — the user invoked /loop without an interval, asking you to self-pace iterations of a specific task. (`14-ScheduleWakeup.json`)
- `SendUserFile` — Send files to the user. Use this for any file the user would want to see — a generated diagram, a report, a screenshot, a built artifact — and you want it surfaced, not just mentio (`15-SendUserFile.json`)
- `SendUserMessage` — Send a message the user will read verbatim. Use this for content they need to see exactly as written between tool calls — a generated code snippet, a specific value, a direct reply (`16-SendUserMessage.json`)
- `ShowOnboardingRolePicker` — Render a clickable role-picker chip row during Cowork onboarding. Call this when asking the user what kind of work they do so they can pick their role and get a matching plugin ins (`17-ShowOnboardingRolePicker.json`)
- `Skill` — Invoke a skill. (`18-Skill.json`)
- `SuggestSkills` — Render a card of standalone skills the user can add — org, shared, or Anthropic skills not yet enabled. (`19-SuggestSkills.json`)
- `TaskCreate` — Use this tool to create a structured task list for your current coding session. This helps you track progress, organize complex tasks, and demonstrate thoroughness to the user. (`20-TaskCreate.json`)
- `TaskUpdate` — Use this tool to update a task in the task list. (`21-TaskUpdate.json`)
- `ToolSearch` — Fetches full schema definitions for deferred tools so they can be called. (`22-ToolSearch.json`)
- `WebFetch` — Fetches a URL, converts the page to markdown, and answers `prompt` against it using a small fast model. (`23-WebFetch.json`)
- `WebSearch` — Search the web. Returns result blocks with titles and URLs. US-only. (`24-WebSearch.json`)
- `Write` — Writes a file to the local filesystem, overwriting if one exists. (`25-Write.json`)
- `mcp__claude_ai__conversation_search` — Search through past user conversations to find relevant context and information (`26-mcp__claude_ai__conversation_search.json`)
- `mcp__claude_ai__current_time` — Returns the current date and time as an ISO 8601 timestamp with its UTC offset, in the person's time zone when it is known and otherwise in UTC. Claude has no clock of its own, so  (`27-mcp__claude_ai__current_time.json`)
- `mcp__claude_ai__end_conversation` — Use this tool to end the conversation. This tool will close the conversation and prevent any further messages from being sent. (`28-mcp__claude_ai__end_conversation.json`)
- `mcp__claude_ai__image_search` — Default to using image search for any query where visuals would enhance the user's understanding; skip when the deliverable is primarily textual e.g. for pure text tasks, code, tec (`29-mcp__claude_ai__image_search.json`)
- `mcp__claude_ai__launch_extended_search_task` — The research tool (AKA compass or the launch_extended_search_task) calls a research agent to perform a comprehensive, agentic search through the web, the user’s Google Drive, and o (`30-mcp__claude_ai__launch_extended_search_task.json`)
- `mcp__claude_ai__read_conversation` — Open one past chat at a conversation_search hit and return a few turns around it. Not for skimming whole chats. Pass conversation_id "current" to re-read earlier turns of this chat (`31-mcp__claude_ai__read_conversation.json`)
- `mcp__claude_ai__recent_chats` — List the user's most recently updated conversations (`32-mcp__claude_ai__recent_chats.json`)
- `mcp__Claude_Docs__batch` — Create a doc, or apply several operations to one doc atomically. (`33-mcp__Claude_Docs__batch.json`)
- `mcp__Claude_Docs__guide` — Docs guides: topic.instructions repeats the server instructions. Read it only if your client dropped them. Also topic.<name>, refusal.<code>. After a doc's birth → ["topic.index"]. (`34-mcp__Claude_Docs__guide.json`)
- `mcp__Claude_Docs__update` — Edit a tab's contents, rename a doc or tab, or change a stored value. (`35-mcp__Claude_Docs__update.json`)
- `mcp__claude-code-remote__create_trigger` — Create a scheduled task. Each firing starts a FRESH SESSION in this environment, never this conversation — the user views each run independently. To schedule a one-off reminder tha (`36-mcp__claude-code-remote__create_trigger.json`)
- `mcp__claude-code-remote__delete_trigger` — Delete a scheduled task. The scheduled task must belong to the calling session's account — deleting another account's scheduled task fails with not-found. Use this to undo a create (`37-mcp__claude-code-remote__delete_trigger.json`)
- `mcp__claude-code-remote__fire_trigger` — Fire a scheduled task immediately, outside of its schedule. The scheduled task must belong to the calling session's account. Use this to kick off a scheduled task on demand — e.g.  (`38-mcp__claude-code-remote__fire_trigger.json`)
- `mcp__claude-code-remote__list_triggers` — List scheduled tasks owned by this account. Use it to find trigger IDs (trig_...) for update_trigger and delete_trigger. From a thread in a Slack channel, only scheduled tasks that (`39-mcp__claude-code-remote__list_triggers.json`)
- `mcp__claude-code-remote__send_later` — Schedule a message to be delivered back into THIS SESSION at a future time. The message arrives as an ordinary user turn, so you can use it to remind yourself to resume work, check (`40-mcp__claude-code-remote__send_later.json`)
- `mcp__claude-code-remote__update_trigger` — Update a scheduled task's name, cron expression, enabled state, model, or prompt. Only provided fields are changed; omit a field to leave it as-is. The scheduled task must belong t (`41-mcp__claude-code-remote__update_trigger.json`)
- `mcp__memory__memory_append` — Add text to the end of a memory document without resending its content. The appended text is placed on a new line after the existing content. Cheaper than memory_write for adding a (`42-mcp__memory__memory_append.json`)
- `mcp__memory__memory_list` — List memory documents (optionally under a path prefix), sorted by path. Returns path, size, and last-updated time for each. Results are capped; use cursor to page through large sto (`43-mcp__memory__memory_list.json`)
- `mcp__memory__memory_read` — Read one or more memory documents. Returns each document's content and last-updated time. Pass a list of paths to read several files in a single call instead of one call per file. (`44-mcp__memory__memory_read.json`)
- `mcp__memory__memory_str_replace` — Edit a memory document by replacing one exact text match. old_str must match the file content in exactly one place, including whitespace and newlines — zero or multiple matches are (`45-mcp__memory__memory_str_replace.json`)
- `mcp__memory__memory_write` — Create or update a memory document with full content. Overwrites if the path already exists: content replaces the ENTIRE document — this is not an append or a patch. Include every  (`46-mcp__memory__memory_write.json`)
- `mcp__widgets__chart_display_v0` — Display a simple chart (line, bar, or scatter) inline in the chat, rendered natively by the app. Use this for quick, standard charts of a small dataset that is already in the conve (`47-mcp__widgets__chart_display_v0.json`)
- `mcp__widgets__comparison_card_display_v0` — Show 2–3 products side-by-side in a comparison table with aligned attribute rows. Use this for shopping questions where the user is weighing a small set of named options against th (`48-mcp__widgets__comparison_card_display_v0.json`)
- `mcp__widgets__featured_card_display_v0` — Show your single best product pick as one rich card with a name, optional price, and a blurb on why it's the pick. Use this for shopping questions where the answer is one clear rec (`49-mcp__widgets__featured_card_display_v0.json`)
- `mcp__widgets__fetch_sports_data` — Use this tool whenever you need to fetch current, upcoming or recent sports data including scores, standings/rankings, and detailed game stats for the provided sports. If a user is (`50-mcp__widgets__fetch_sports_data.json`)
- `mcp__widgets__itinerary_display_v0` — Show a day-by-day travel timeline with tabbed days and a list of stops per day. Use this for trip-planning questions where the answer is an ordered itinerary across one or more day (`51-mcp__widgets__itinerary_display_v0.json`)
- `mcp__widgets__link_preview_display_v0` — Show 1–6 web links as preview cards with title, source, and an optional snippet. Use this when surfacing external web sources the user should open — search results, citations, or ' (`52-mcp__widgets__link_preview_display_v0.json`)
- `mcp__widgets__message_compose_v1` — Draft a message (email, Slack, or text) with goal-oriented approaches based on what the user is trying to accomplish. Analyze the situation type (work disagreement, negotiation, fo (`53-mcp__widgets__message_compose_v1.json`)
- `mcp__widgets__options_card_display_v0` — Show a structured set of distinct approaches the user could take, each with concrete next steps. Use this for personal-health questions where the answer is 2–6 alternative options  (`54-mcp__widgets__options_card_display_v0.json`)
- `mcp__widgets__places_list_display_v0` — Show a stacked list of places, each with up to 3 photos and a short description. Use this when the answer is a browsable set of 2–8 specific places the user might visit — cafes, hi (`55-mcp__widgets__places_list_display_v0.json`)
- `mcp__widgets__places_map_display_v0` — Display locations on a map with your recommendations and insider tips. (`56-mcp__widgets__places_map_display_v0.json`)
- `mcp__widgets__places_search` — Search for places, businesses, restaurants, and attractions using Google Places. (`57-mcp__widgets__places_search.json`)
- `mcp__widgets__product_carousel_display_v0` — Show a paged product carousel — one product per page, each with a 3-photo strip, name, price, and a short blurb. Use this for shopping questions where the user wants to look closel (`58-mcp__widgets__product_carousel_display_v0.json`)
- `mcp__widgets__quiz_display_v0` — Generate an interactive multiple-choice quiz rendered as a card in the chat; the same questions can also be flipped through as flashcards (question on the front, correct answer and (`59-mcp__widgets__quiz_display_v0.json`)
- `mcp__widgets__recipe_display_v0` — Display an interactive recipe with adjustable servings. Use when the user asks for a recipe, cooking instructions, or food preparation guide. The widget allows users to scale all i (`60-mcp__widgets__recipe_display_v0.json`)
- `mcp__widgets__step_card_display_v0` — Show a numbered, step-by-step walkthrough for fixing or setting something up. Use this for tech-support and how-to questions where the answer is 3–8 ordered steps, each with a shor (`61-mcp__widgets__step_card_display_v0.json`)
- `mcp__widgets__translation_display_v0` — Show a translation card when the user asks how to say, write or translate a specific short passage (a message, sentence, phrase or a few lines) into another language. The card show (`62-mcp__widgets__translation_display_v0.json`)
- `mcp__widgets__weather_fetch` — Display weather information. Use the user's home location to determine temperature units: Fahrenheit for US users, Celsius for others.<br><br>USE THIS TOOL WHEN:<br>- User asks abo (`63-mcp__widgets__weather_fetch.json`)
